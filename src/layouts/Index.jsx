import React, { Component } from 'react';

import Nav from '@/components/command/Nav';

export default class Layout extends Component {
    render() {
        return (
            <div><Nav /></div>
        );
    }
}
